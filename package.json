{
  "name": "generator-github-create",
  "version": "0.3.0",
  "description": "Yeoman generator for github authentication, create repository, README Badges, local git initialization, and local commit and push.",
  "keywords": [
    "yeoman-generator",
    "scaffold",
    "github",
    "repository",
    "create",
    "create repository",
    "organization",
    "git",
    "authentication",
    "authenticate",
    "badge",
    "badges"
  ],
  "main": "generators/app/index.js",
  "files": [
    "generators/app",
    "generators/authenticate",
    "generators/create",
    "generators/orgs",
    "generators/gitinit",
    "generators/gitpush",
    "generators/readme"
  ],
  "engines": {
    "node": ">=6.4",
    "npm": ">=3.10.3"
  },
  "scripts": {
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "build": "rm -rf ./generators && babel --out-dir ./generators --extensions .es6 --copy-files ./src/generators",
    "build:watch": "rm -rf ./generators && babel --watch --out-dir ./generators --extensions .es6 --copy-files ./src/generators",
    "lint": "eslint --ignore-pattern **/templates ./tests/** ./src/**",
    "postversion": "git push && git push --tags && npm publish --access public",
    "release:patch": "npm test && npm run build && npm version patch",
    "release:minor": "npm test && npm run build && npm version minor",
    "release:major": "npm test && npm run build && npm version major",
    "test": "npm run lint && cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text mocha --compilers js:babel-core/register --require test/mocha.setup.js test/**/*.spec.js",
    "test:watch": "npm run lint && cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text mocha --watch --compilers js:babel-core/register --require test/mocha.setup.js test/**/*.spec.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trainerbill/generator-github-create.git"
  },
  "author": "Andrew Throener",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/trainerbill/generator-github-create/issues"
  },
  "homepage": "https://github.com/trainerbill/generator-github-create#readme",
  "dependencies": {
    "github": "^2.5.2",
    "lodash.find": "^4.6.0",
    "lodash.merge": "^4.6.0",
    "shelljs": "^0.7.4",
    "winston": "^2.2.0",
    "yeoman-generator": "^0.24.1",
    "yosay": "^1.2.0"
  },
  "peerDependencies": {},
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-core": "^6.14.0",
    "babel-plugin-istanbul": "^2.0.1",
    "babel-preset-es2015-node6": "^0.3.0",
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "coveralls": "^2.11.12",
    "cross-env": "^2.0.1",
    "eslint": "^3.4.0",
    "mocha": "^3.0.2",
    "nyc": "^8.1.0",
    "sinon": "^1.17.5",
    "sinon-as-promised": "^4.0.2",
    "sinon-chai": "^2.8.0",
    "yeoman-assert": "^2.2.1",
    "yeoman-test": "^1.4.0"
  }
}
